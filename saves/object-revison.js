// Object

const person = {
  name: 'John',
  age: 30,
  weight: 88.6,
  isAdmin: true
}

// console.log(person) pega o objeto todo
// console.log(person.name) //pega somente a propriedade nome
console.log(`${person.name} tem ${person.age} anos`)